/**
 * Claude Code adapter entry. Subprocess-based; requires the `claude`
 * CLI in PATH (or override via `claudeExecutable`).
 *
 * @internal
 */

export { claudeCodeAgent, type ClaudeCodeAgentConfig } from './adapter.js';
